import React from 'react';
import PropTypes from 'prop-types';

page.propTypes = {
    
};

function page() {
    return (
        <div>
            <p>This is about page</p>
        </div>
    );
}

export default page;